export pizza to "http://example.org/pizza"

import "http://example.org/restaurant"

param namePrefix : EString

from Restaurant r
where r.name.startsWith(namePrefix)
create {
    it.name
    it.sells create {
        it.name
        it.price
    }
}
