export pizza to "http://example.org/pizza"

import "http://example.org/restaurant"

param selectedFoods : EList<Food>

from Restaurant r
where r.sells.exists(s | selectedFoods.exists(sf | sf.name == s.name))
create {
    it.name
    it.sells create {
        it.name
        it.price
    }
}
